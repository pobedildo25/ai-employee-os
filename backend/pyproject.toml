[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
# Critical correctness only (CI gate). Broader style lint is out of scope for Stage 24.
select = ["E9", "F63", "F7", "F82"]
ignore = []
